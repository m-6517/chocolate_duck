<% content_for(:title, t('.title')) %>
<div class="flex items-center justify-center p-5">
  <div class="max-w-md w-full mx-auto bg-slate-50 p-6 rounded-lg shadow-lg">
    <div class="flex items-center space-x-4">
      <h1 class="text-2xl font-medium mb-6"><%= t('.title') %></h1>
      <i class="fa-regular fa-circle-question text-xl cursor-pointer mb-6" onclick="document.getElementById('my_modal_1').showModal()"></i>
      <dialog id="my_modal_1" class="modal">
        <div class="modal-box bg-slate-50">
          <p class="py-4">画像はちゃいろだけ、きいろだけでもOK！</p>
          <p class="py-4">ちゃいろ度・きいろ度を判定するつもりでしたが、表示されなくなったので準備中です！（今は0%で表示されます）</p>
          <div class="modal-action">
            <form method="dialog">
              <button class="btn">とじる</button>
            </form>
          </div>
        </div>
      </dialog>
    </div>
    <%= render 'form', board: @board %>
  </div>
</div>
