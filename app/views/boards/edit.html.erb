<% content_for(:title, t('.title')) %>
<div class="flex items-center justify-center p-5">
  <div class="max-w-lg w-full mx-auto">
    <h1 class="text-2xl font-mplus_rounded_1c font-semibold mb-6"><%= t('.title') %></h1>
    <%= render 'form', board: @board %>
  </div>
</div>
